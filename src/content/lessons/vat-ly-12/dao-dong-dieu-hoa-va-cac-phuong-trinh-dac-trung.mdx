---
title: "Dao động điều hòa và các phương trình đặc trưng"
subject: "vat-ly-12"
subject_name: "Vật lý 12"
description: "Lý thuyết cơ bản về dao động điều hòa, phương trình li độ, vận tốc và gia tốc."
video_url: "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
tags: ["Cơ học", "Dao động", "Sóng cơ"]
created_at: "2026-06-08T09:10:00Z"
order: 1
---

## 1. Định nghĩa dao động điều hòa
Dao động điều hòa là dao động trong đó li độ của vật là một hàm côsin (hoặc sin) của thời gian.

## 2. Phương trình dao động điều hòa
Li độ của vật dao động điều hòa có dạng:
$$x = A \cos(\omega t + \varphi)$$
Trong đó:
- $A$ là biên độ dao động ($A > 0$), đơn vị mét (m) hoặc xentimét (cm).
- $\omega$ là tần số góc (rad/s).
- $\varphi$ là pha ban đầu (rad).
- $(\omega t + \varphi)$ là pha của dao động tại thời điểm $t$ (rad).

## 3. Phương trình vận tốc và gia tốc
- Vận tốc $v$ là đạo hàm bậc nhất của li độ theo thời gian:
  $$v = x' = -\omega A \sin(\omega t + \varphi) = \omega A \cos(\omega t + \varphi + \frac{\pi}{2})$$
  Vận tốc sớm pha $\frac{\pi}{2}$ so với li độ.
- Gia tốc $a$ là đạo hàm bậc hai của li độ theo thời gian:
  $$a = v' = x'' = -\omega^2 A \cos(\omega t + \varphi) = -\omega^2 x$$
  Gia tốc ngược pha so với li độ (sớm pha $\frac{\pi}{2}$ so với vận tốc).
